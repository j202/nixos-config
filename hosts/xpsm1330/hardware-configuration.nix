# Copy the laptop's generated hardware config here:
#   cp /etc/nixos/hardware-configuration.nix hosts/xpsm1330/hardware-configuration.nix
#
# If starting fresh, regenerate it on the laptop with:
#   nixos-generate-config --show-hardware-config > hosts/xpsm1330/hardware-configuration.nix

{ ... }: { }
